spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/xmldb_dev
    username: postgres
    password: dev_password

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.xmlservice.xmlservice: DEBUG
    org.springframework.jdbc.core.JdbcTemplate: DEBUG